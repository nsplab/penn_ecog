# input sampling rate, Hz, to the signal acquisition module
inSampleRate = 24400
# output sampling rate, Hz, to the signal acquisition module
outSampleRate = 4166
# number of ECoG channels only that are sent to the feature extraction module. The signal acquisition module allows for additional channels (force sensor, etc.).
numChannels = 60
# in the data array sent by the signal acquisition module what is the index of the first neural signal channel
# in the TDT module this would be 4, as the 5th element is the first neural signal channel and indexing starts from zero/0
neuralIndex = 0
